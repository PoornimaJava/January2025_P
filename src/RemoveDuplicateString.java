/**
 * write a program to print after remove duplicate from a string
 */

public class RemoveDuplicateString {
    public static void main(String[] args) {
        String str = "this is new value";
        char[] ch = str.toCharArray();
        char empty;
        for(Character c : ch) {

        }
    }
}
